# Create a new SSH key
resource "digitalocean_ssh_key" "{{ ssh_key_name }}" { # Value provided by Jinja
  name       = "{{ ssh_key_name }}" # Value provided by Jinja
  public_key = "{{ ssh_pub_key.decode('utf-8') }}" # Value provided by Jinja
}
